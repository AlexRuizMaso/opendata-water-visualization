{
  "name": "opendata-water-visualization",
  "version": "1.0.0",
  "description": "Aplicació web per a la visualització de dades obertes sobre recursos hídrics de Catalunya",
  "private": true,
  "workspaces": [
    "frontend",
    "etl"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "etl": "npm run dev --workspace=etl",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=etl",
    "format": "npm run format --workspace=frontend && npm run format --workspace=etl",
    "test": "npm run test --workspace=frontend && npm run test --workspace=etl",
    "test:frontend": "npm run test --workspace=frontend",
    "test:etl": "npm run test --workspace=etl"
  },
  "keywords": [
    "water",
    "data-visualization",
    "opendata",
    "Catalunya",
    "ETL"
  ],
  "author": "Àlex",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7"
  }
}
